# Template: Codex Handoff Summary

> Closing report shape for a fix applied in response to a Codex finding
> or a precision-fix request (`.claude/commands/precision-fix.md`). Keep
> it procedural: what changed and what was verified — not a re-argument
> of the finding itself.

# Handoff Summary: [Finding / Task]

## 1. Files Changed

| File | Change |
|---|---|
| | |

## 2. Confirmation: `docs/` Unchanged

[State explicitly whether any `docs/` file was touched. If none, say so
plainly.]

## 3. Confirmation: Repository Truth Not Duplicated

[State whether the fix removed or avoided restating any fact that
`docs/DOCUMENT-INDEX.md` or another `docs/` document already owns —
e.g. document lifecycle status.]

## 4. Confirmation: No New Governance Introduced

[State whether any new rule, boundary, or decision was introduced. A
precision fix should normally answer "none" here.]

## 5. Follow-Up (Optional)

Anything the fix surfaced but did not address, because it was out of the
requested scope.
